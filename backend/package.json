{
  "name": "atomquest-backend",
  "version": "1.0.0",
  "scripts": {
    "build": "pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py seed_reference_data && python manage.py seed_test_users"
  }
}
