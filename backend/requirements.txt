# ── Core ──────────────────────────────────────────────────────────────────
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1

# ── Date utilities (pure Python) ──────────────────────────────────────────
python-dateutil==2.8.2

# ── Excel export — used in portal/utils.py ────────────────────────────────
openpyxl==3.1.2

# ── Production server (pure Python) ──────────────────────────────────────
gunicorn==21.2.0

# ── NOT included (require C compiler or not used locally) ─────────────────
# psycopg2-binary  — only needed for PostgreSQL; SQLite works out of the box
# pandas           — not imported anywhere in the codebase
# celery           — no tasks dispatched at runtime
# redis            — no runtime usage
# python-dotenv    — not imported anywhere in the codebase
